{{ 'lom-natural.css' | asset_url | stylesheet_tag }}

<section class="lom-ticker">
  <div class="lom-ticker__track" id="lom-ticker-track">
    {% for block in section.blocks %}
      <span class="lom-ticker__item" {{ block.shopify_attributes }}>
        <span class="lom-ticker__dot"></span>
        {{ block.settings.text }}
      </span>
    {% endfor %}
    {% for block in section.blocks %}
      <span class="lom-ticker__item">
        <span class="lom-ticker__dot"></span>
        {{ block.settings.text }}
      </span>
    {% endfor %}
  </div>
</section>

{% schema %}
{
  "name": "Lom Social Ticker",
  "settings": [],
  "blocks": [
    {
      "type": "item",
      "name": "Ticker item",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "★★★★★ Sarah from NY just purchased"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Lom Social Ticker",
      "blocks": [
        { "type": "item", "settings": { "text": "★★★★★ 2,847+ five-star reviews" } },
        { "type": "item", "settings": { "text": "🔥 50,000+ units sold worldwide" } },
        { "type": "item", "settings": { "text": "🇺🇸 Free US & EU shipping" } },
        { "type": "item", "settings": { "text": "✓ 100% natural ingredients" } },
        { "type": "item", "settings": { "text": "🛡️ 30-day money-back guarantee" } },
        { "type": "item", "settings": { "text": "✈️ TSA-friendly — take it anywhere" } }
      ]
    }
  ]
}
{% endschema %}
