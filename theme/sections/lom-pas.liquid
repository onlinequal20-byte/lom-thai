{{ 'lom-natural.css' | asset_url | stylesheet_tag }}

<section class="lom-pas">
  <div class="lom-container">
    <h2 class="lom-heading lom-fade-in">{{ section.settings.heading }}</h2>
    {% if section.settings.subtitle != blank %}
      <p class="lom-heading-subtitle lom-fade-in">{{ section.settings.subtitle }}</p>
    {% endif %}
    <div class="lom-pas-grid">
      {% for block in section.blocks %}
        <div class="lom-pas-card lom-fade-in" {{ block.shopify_attributes }}>
          <div class="lom-pas-icon">{{ block.settings.icon }}</div>
          <h3>{{ block.settings.heading }}</h3>
          <p>{{ block.settings.text }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Lom Problem-Solution",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Sound Familiar?"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle"
    }
  ],
  "blocks": [
    {
      "type": "card",
      "name": "Card",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (emoji)",
          "default": "🌿"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "The Problem"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Description",
          "default": "Drowsy commutes, afternoon slumps, brain fog, tension headaches — you're tired of feeling tired."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Lom Problem-Solution",
      "blocks": [
        { "type": "card", "settings": { "icon": "😴", "heading": "The Problem", "text": "Drowsy commutes, afternoon slumps, brain fog, sinus pressure, tension headaches — you're tired of feeling tired." } },
        { "type": "card", "settings": { "icon": "🚫", "heading": "Why Others Fail", "text": "Energy drinks crash. Coffee gives jitters. Pharmacy sprays cause rebound. Pills take 30 minutes. None of them are natural." } },
        { "type": "card", "settings": { "icon": "🌿", "heading": "The Siam Lom Way", "text": "One inhale = instant relief. 100% natural Thai herbs, proven for a century. No crash, no chemicals, no waiting. Just breathe." } }
      ]
    }
  ]
}
{% endschema %}
